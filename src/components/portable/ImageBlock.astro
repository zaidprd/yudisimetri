---
import { urlFor } from '../../lib/sanity';
const { node } = Astro.props;
const src = node?.asset ? urlFor(node.asset).width(800).url() : null;
const alt = node?.alt || '';
---
{src && (
  <img src={src} alt={alt} loading="lazy" />
)}
